# Статус: СПН UX iteration 2026-06-23 14:15

## Контекст

Проверяли личный кабинет и карточку сделки для роли СПН на примере профиля:

- Овчинников Александр Константинович
- a.k.ovchinnikov@borisoglebsk.etagi.com
- role: spn
- user id: 98ee4523-dacb-47c3-b458-97e524f92444

Цель итерации: убрать из интерфейса СПН действия, которые пользователь не может выполнить по backend-правам, и оставить понятный контроль чужих задач.

## Изменения

- `assets/js/nav-v2/deal-card-v2.js`
  - Добавлена проверка `canChangeTaskStatus(task)` по роли, пользователю, `assigned_to` и `assigned_role`.
  - Для задач, доступных текущему пользователю, показываются кнопки смены статуса: `В работе`, `Готово`, `Открыта`.
  - Для задач другой роли кнопки скрываются, вместо них показывается пояснение: задача видна для контроля, но статус меняет ответственный специалист.
  - Ролевые метки задач теперь показываются человекочитаемо: `СПН`, `юрист`, `брокер`.

- `deal-card-v2.html`
  - Версия основного скрипта карточки обновлена до `deal-card-v2.js?v=20260623-1415`, чтобы браузер не использовал старый кеш.

## Проверка Supabase

Пользовательский JWT-контекст:

- `request.jwt.claim.role = authenticated`
- `request.jwt.claim.sub = 98ee4523-dacb-47c3-b458-97e524f92444`

Проверка `public.nav_v2_can_change_task_status(t.id)` по сделке `c290477b-aef3-4523-ae25-8d29f02b9552`:

| Задача | Роль задачи | СПН Овчинников может менять |
| --- | --- | --- |
| Проверка банка / ипотеки / маткапитала | broker | false |
| Согласовать порядок расчетов | spn | true |
| Юридическая проверка до задатка | lawyer | false |

Также проверено:

- `nav_v2_get_my_profile()` возвращает `id = 98ee4523-dacb-47c3-b458-97e524f92444`, `role = spn`.
- `nav_v2_get_deal_card(...)` возвращает тот же профиль в поле `profile`.

## Итог

Карточка сделки для СПН теперь не провоцирует ошибочные действия: СПН видит задачи юриста и брокера для контроля, но не получает кнопки изменения их статусов. Backend остается источником истины: серверная функция прав продолжает блокировать недоступные изменения.
