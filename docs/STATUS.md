# Статус проекта «Навигатор сделок»

## Актуально на 14 июня 2026

- Репозиторий проекта определён: `deputat36/vktg`.
- Основная ветка: `main`.
- Supabase-проект: `ofewxuqfjhamgerwzull`.
- После восстановления Supabase авторизация в CRM снова работает.
- PR #10 смержен: исправлен прыжок страницы вверх при изменении статуса документа в карточке сделки.
- Статус документа сохраняется и не сбрасывается после обновления страницы.
- Исправлено отображение статусов документов после перезагрузки карточки: `needed`, `received`, `checked` переводятся в интерфейсе как `Нужен`, `Получен`, `Проверен`.

## Проверки текущего изменения

- Технические значения, отправляемые в Supabase, не изменены.
- SQL, миграции, таблицы и RPC не изменялись.
- Роли, меню, рабочий стол и список сделок не изменялись.
- Синтаксис `assets/js/nav-v2/deal-card-doc-inline-v2.js` проверен командой `node --check` — ошибок нет.
- В `deal-card-v2.html` обновлена версия подключаемого скрипта для обхода кеша браузера.

## Важные ограничения

- Не мержить широкие PR #8 и #9: они затрагивают слишком много файлов и могут вернуть нестабильность.
- При следующих изменениях использовать правила из `AGENTS.md`, `docs/PROJECT.md` и `docs/DECISIONS.md`.
