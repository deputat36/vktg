# Статус итерации СПН UX — 2026-06-24 07:40 CEST

## Цель

Позволить СПН восстановить автоматическую проверку дублей после временной ошибки без перезагрузки страницы и потери контекста работы.

## Изменения

- Одноразовый флаг загрузки заменён на состояния: запрос выполняется, запрос выполнен, данные загружены.
- После ошибки доступна кнопка «Повторить проверку».
- Повторный RPC выполняется на текущей странице; локальный черновик не очищается.
- Во время запроса кнопка блокируется и показывает «Проверяю...».
- Параллельные повторные запросы не запускаются.
- После успешного ответа предупреждение исчезает либо заменяется найденными совпадениями.
- Добавлена отдельная ссылка «Открыть мои сделки» для ручной проверки в новой вкладке.
- Перед повторной проверкой старое подтверждение сохранения без проверки сбрасывается.
- Версия `spn-duplicate-deal-guard-v2.js` обновлена до `20260624-0740`.

## Проверка Supabase

Для Овчинникова Александра Константиновича:

- роль списка: `spn`;
- доступно сделок: `3`;
- роль тестовой карточки: `spn`;
- данные мастера в карточке доступны.

Схема и данные Supabase не изменялись.

CRM «Лидер» не изменялась.