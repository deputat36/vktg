# СПН UX — итерация 2026-06-23 21:25 CEST

## Цель

Убрать задержку и лишний повторный RPC при обновлении подсказок «Первым делом СПН» в списке сделок.

## Изменения

- `deals-v2.js` после успешной загрузки и отрисовки публикует событие `nav-v2:deals-loaded` с профилем и актуальным списком сделок.
- Последний загруженный набор также доступен через `window.navV2Deals`, чтобы поздно подключившийся модуль не выполнял повторный запрос.
- `deals-spn-priority-hints-v2.js` использует опубликованные данные и сразу обновляет подсказки.
- При нажатии «Обновить» резервный RPC запускается только если основная загрузка не опубликовала событие.
- Версии JS в `deals-v2.html` обновлены до `20260623-2125`.

## Проверка

Под профилем Овчинникова Александра Константиновича (`spn`) RPC `nav_v2_get_deals_list` вернул:

- сделок: 3;
- сделок с адресом: 2;
- демо-сделок: 0.

CRM «Лидер» не изменялась.