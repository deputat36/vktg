import { rpc, esc } from './supabase-v2.js';

const id = new URLSearchParams(location.search).get('id');
const PANEL = 'spnReturnViewV2';
function arr(v){return Array.isArray(v)?v:[]}
function textOf(c){return String(c?.body||'')}
function lastText(data){const c=arr(data?.comments).find(x=>textOf(x).includes('Юрист вернул')||textOf(x).includes('Что нужно доработать'))||arr(data?.comments)[0];return textOf(c)||'Юрист вернул сделку на доработку. Проверьте комментарии и заполните недостающие данные.'}
function items(body){const lines=String(body||'').split('\n').map(x=>x.trim()).filter(Boolean);const nums=lines.filter(x=>/^\d+[.)]\s+/.test(x));return nums.length?nums:lines.filter(x=>x.includes('документ')||x.includes('риск')||x.includes('расч')||x.includes('расход')||x.includes('уточн')||x.includes('доработ')).slice(0,8)}
function html(data){const body=lastText(data);const list=items(body);return `<section class="card" id="${PANEL}" style="border:2px solid rgba(245,158,11,.35);background:#fffdf7"><div class="section-title"><div><span class="pill yellow">доработка</span><h2 style="margin:8px 0 4px">Что исправить по сделке</h2><p class="muted" style="margin:0">Юрист вернул карточку на доработку. Исправьте пункты и отправьте на повторную проверку.</p></div><span class="pill yellow">need_info</span></div><div class="grid"><div class="card" style="box-shadow:none"><h3>Чек-лист</h3>${list.length?`<div class="list">${list.map(x=>`<div class="list-item"><b>□</b><span>${esc(x.replace(/^\d+[.)]\s+/,''))}</span></div>`).join('')}</div>`:'<div class="status warn">Прочитайте полный комментарий юриста справа.</div>'}</div><div class="card" style="box-shadow:none"><h3>Комментарий юриста</h3><textarea readonly style="min-height:230px">${esc(body)}</textarea><div class="field"><label>Комментарий СПН после исправления</label><textarea id="spnReturnTextV2">Заявка доработана. Прошу повторно проверить.</textarea></div><div id="spnReturnStatusV2" class="status">После исправления отправьте сделку юристу повторно.</div><div class="actions" style="justify-content:flex-start"><button class="btn primary" id="spnReturnSendV2" type="button">Отправить юристу повторно</button></div></div></div></section>`}
function bind(){const b=document.getElementById('spnReturnSendV2');if(!b)return;b.onclick=async()=>{const status=document.getElementById('spnReturnStatusV2');const body=document.getElementById('spnReturnTextV2')?.value||'Заявка доработана. Прошу повторно проверить.';if(!confirm('Отправить сделку юристу на повторную проверку?'))return;b.disabled=true;status.className='status warn';status.textContent='Отправляю...';try{await rpc('nav_v2_submit_spn_rework',{p_deal_id:id,p_body:body},15000);status.className='status ok';status.textContent='Отправлено. Обновляю карточку...';setTimeout(()=>location.reload(),700)}catch(e){b.disabled=false;status.className='status error';status.textContent=e.message||String(e)}}}
async function load(){if(!id)return;try{const data=await rpc('nav_v2_get_deal_card',{p_deal_id:id},45000);if(data?.deal?.status!=='need_info')return;if(document.getElementById(PANEL))return;const main=document.querySelector('#app main');if(!main)return;const summary=document.getElementById('legalSummaryV2');(summary||main.firstElementChild||main).insertAdjacentHTML('afterend',html(data));bind()}catch(e){console.warn('spn return view skipped',e)}}
let tries=0;const timer=setInterval(()=>{tries++;if(document.querySelector('#app main')){clearInterval(timer);load()}if(tries>40)clearInterval(timer)},150);
