name: Navigator v2 index write/storage measurement v1

on:
  pull_request:
    paths:
      - 'config/nav-v2-index-write-storage-measurement-v1.json'
      - 'config/nav-v2-query-to-index-mapping-v1.json'
      - 'config/nav-v2-index-fk-parent-mutation-evidence-v1.json'
      - 'tests/sql/nav_v2_index_write_storage_measurement_harness_v1.sql'
      - 'scripts/check_nav_v2_index_write_storage_measurement_v1.py'
      - 'docs/NAV_V2_INDEX_WRITE_STORAGE_MEASUREMENT_V1_2026-07-22.md'
      - '.github/workflows/nav-v2-index-write-storage-measurement-v1.yml'
  push:
    branches: [main]
    paths:
      - 'config/nav-v2-index-write-storage-measurement-v1.json'
      - 'config/nav-v2-query-to-index-mapping-v1.json'
      - 'config/nav-v2-index-fk-parent-mutation-evidence-v1.json'
      - 'tests/sql/nav_v2_index_write_storage_measurement_harness_v1.sql'
      - 'scripts/check_nav_v2_index_write_storage_measurement_v1.py'
      - 'docs/NAV_V2_INDEX_WRITE_STORAGE_MEASUREMENT_V1_2026-07-22.md'
      - '.github/workflows/nav-v2-index-write-storage-measurement-v1.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Validate measurement JSON
        run: python3 -m json.tool config/nav-v2-index-write-storage-measurement-v1.json > /dev/null

      - name: Compile measurement validator
        run: python3 -m py_compile scripts/check_nav_v2_index_write_storage_measurement_v1.py

      - name: Check write/storage measurement source contract
        run: python3 scripts/check_nav_v2_index_write_storage_measurement_v1.py

  postgres-17-write-storage-measurement:
    needs: source-contract
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: navigator_write_storage
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d navigator_write_storage"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      PGHOST: 127.0.0.1
      PGPORT: 5432
      PGDATABASE: navigator_write_storage
      PGUSER: postgres
      PGPASSWORD: postgres
    steps:
      - uses: actions/checkout@v4

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Wait for PostgreSQL 17
        run: |
          for attempt in $(seq 1 30); do
            if pg_isready -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE"; then exit 0; fi
            sleep 2
          done
          exit 1

      - name: Run synthetic write/storage measurement and rollback
        run: |
          set -o pipefail
          psql -v ON_ERROR_STOP=1 -f tests/sql/nav_v2_index_write_storage_measurement_harness_v1.sql \
            | tee /tmp/nav-v2-index-write-storage-measurement-v1.log

      - name: Upload synthetic write/storage evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nav-v2-index-write-storage-measurement-v1
          path: /tmp/nav-v2-index-write-storage-measurement-v1.log
          if-no-files-found: warn
          retention-days: 30
