name: Navigator v2 live production smoke

on:
  workflow_dispatch:
  schedule:
    - cron: '17 5 * * *'
  push:
    branches:
      - main
    paths:
      - '**-v2.html'
      - 'nav-v2.html'
      - 'assets/js/nav-v2/**'
      - 'assets/css/**'
      - 'config/nav-v2-module-budget.json'
      - 'scripts/check_nav_v2_edge_auth.py'
      - 'scripts/check_nav_v2_live_pages.py'
      - 'supabase/config.toml'
      - 'supabase/functions/nav-v2-deal-api/**'
      - 'supabase/functions/nav-invite-user/**'
      - '.github/workflows/nav-v2-live-pages.yml'

permissions:
  contents: read

concurrency:
  group: nav-v2-live-production-smoke
  cancel-in-progress: true

jobs:
  live-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Check deployed Navigator v2 pages
        env:
          NAV_V2_BASE_URL: https://deputat36.github.io/vktg/
        run: python3 scripts/check_nav_v2_live_pages.py
      - name: Check live Navigator v2 Edge authentication
        env:
          NAV_V2_SUPABASE_URL: https://ofewxuqfjhamgerwzull.supabase.co
        run: python3 scripts/check_nav_v2_edge_auth.py
